# Redirect default Netlify subdomain to primary domain
https://kernvalley-us.netlify.com/* https://kernvalley.us/:splat 301!

# These extra rules are required because HTTPS is not forced
http://kernvalley-us.netlify.com/* https://kernvalley.us/:splat 301!
